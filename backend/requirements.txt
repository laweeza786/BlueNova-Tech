# ─────────────────────────────────────────────────────────────
# BlueNova Tech ERP — Python Dependencies
# Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# Core Framework
Django==6.0.6
asgiref==3.11.1
sqlparse==0.5.5

# Production Server
gunicorn==21.2.0

# PostgreSQL Driver (psycopg 3 with standalone binaries)
psycopg[binary]>=3.1.8

# Database URL parser (for Railway's DATABASE_URL)
dj-database-url==2.1.0

# Static Files (serve CSS/JS in production without a separate web server)
whitenoise==6.6.0

# Media Storage
cloudinary==1.45.0
django-cloudinary-storage==0.3.0

# Environment Variables
python-dotenv==1.2.2

# Image Processing (profile pictures, resume photos)
pillow==12.3.0

# Resume Builder — RenderCV PDF generation
rendercv==2.8
rendercv-fonts==0.5.1
typst==0.15.0
reportlab==5.0.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Markdown==3.10.2
ruamel.yaml==0.19.1

# Data Validation (used by RenderCV)
pydantic==2.13.4
pydantic-extra-types==2.11.1
pydantic_core==2.46.4
annotated-types==0.7.0
typing_extensions==4.16.0
typing-inspection==0.4.2

# Phone & Email Validation
phonenumbers==9.0.34
email-validator==2.3.0

# Utilities
tzdata==2026.2
idna==3.18
charset-normalizer==3.4.7
dnspython==2.8.0
